# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.19
pydantic

# --- Async messaging (RabbitMQ) ---
aio-pika==9.5.4

# --- ASR model ---
torch
torchaudio
# CPU wheels by default. For a GPU box, install a matching torch build
# BEFORE this file with --no-deps, per the model's own install notes:
#   pip install torch==2.5.1 torchaudio==2.5.1 --index-url https://download.pytorch.org/whl/cu121
#   pip install -r requirements.txt --no-deps  (then re-add non-torch deps if needed)
qwencleo-asr==0.2.1
qwen-asr>=0.0.6
numpy
soundfile
huggingface_hub